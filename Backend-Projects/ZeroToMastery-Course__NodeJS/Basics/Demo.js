// Learnt about the process object and this is exammple of process.argv[]

const mission = process.argv[2];    // Argument is at second index of array

if (mission === 'learn') {
    console.log('Time to write');
} else {
    
}